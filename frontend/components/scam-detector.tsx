'use client'
import { useRef, useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { ArrowRight, CheckCircle2, FileSearch, ImagePlus, Link2, Menu, ShieldCheck, Sparkles, UploadCloud, X, Loader2 } from 'lucide-react'
import { checkInput, signIn, signUp, type CheckResult } from '@/lib/mock-api'

function Mark(){return <div className="mark"><ShieldCheck size={18}/><span>SCAM DETECTOR</span></div>}
function Header(){const [open,setOpen]=useState(false);return <header className="site-header"><Link href="/"><Mark/></Link><nav className={open?'nav open':'nav'}><a href="/#how-it-works">How it works</a><a href="/#about">About</a><Link href="/dashboard">Dashboard</Link><Link className="nav-cta" href="/login">Sign in <ArrowRight size={14}/></Link></nav><button className="menu-button" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></header>}
function Verdict({result,onReset}:{result:CheckResult;onReset:()=>void}){const danger=result.verdict!=='safe';return <section className="verdict"><div className="verdict-top"><span className={`status-badge ${danger?'danger':'safe'}`}>{result.verdict.toUpperCase()}</span><span className="confidence">{result.confidence}% confidence</span></div><div className="verdict-title"><span className={danger?'danger-dot':'safe-dot'}/><div><p className="eyebrow">VERDICT</p><h2>{result.verdict==='safe'?'Looks safe':result.verdict==='suspicious'?'Needs a closer look':'Likely a scam'}</h2></div></div><p className="verdict-copy">{result.summary}</p><div className="risk-meter"><span className={result.risk_score>20?'filled':''}/><span className={result.risk_score>40?'filled':''}/><span className={result.risk_score>60?'filled':''}/><span className={result.risk_score>80?'filled':''}/><span className={result.risk_score>90?'filled':''}/></div><div className="finding-list">{result.reasons.map(reason=><p key={reason}><CheckCircle2 size={14}/>{reason}</p>)}</div><button className="text-button" onClick={onReset}>← Check another</button></section>}
function HowItWorks(){const steps=[['1',FileSearch,'Paste a link or message','Share the link or message that made you pause.'],['2',Sparkles,'We analyze it instantly','We compare the signal against common fraud patterns.'],['3',ShieldCheck,'Get a clear verdict','See the answer and the exact reasons behind it.']];return <section className="how-section" id="how-it-works"><div className="section-heading centered"><p className="eyebrow">SIMPLE • FAST • FREE</p><h2>How it works</h2><p>From uncertainty to a clear next step in one calm, guided flow.</p></div><div className="process-flow">{steps.map(([number,Icon,title,copy])=><article className="process-step" key={String(number)}><div className="process-icon"><span className="step-badge">{number}</span><Icon size={30}/></div><h3>{title}</h3><p>{copy}</p></article>)}</div></section>}
function Checker(){const [mode,setMode]=useState<'input'>('input');const [inputType,setInputType]=useState<'link'|'message'>('link');const [value,setValue]=useState('');const [file,setFile]=useState<File|null>(null);const [preview,setPreview]=useState('');const [loading,setLoading]=useState(false);const [result,setResult]=useState<CheckResult|null>(null);const fileRef=useRef<HTMLInputElement>(null);const submit=async()=>{if((mode==='input'&&!value.trim())||(mode==='screenshot'&&!file))return;setLoading(true);const response=await checkInput(mode==='screenshot'?file?.name||'uploaded screenshot':value,inputType==='message'?'message':mode);setResult(response);setLoading(false)};const reset=()=>{setValue('');setFile(null);setPreview('');setResult(null);setLoading(false)};return <div className="checker-wrap"><div className="checker-tabs"><button className={mode==='input'?'active':''} onClick={()=>{setMode('input');setInputType('link');setResult(null)}}><Link2 size={16}/> URL or message</button></div>{result?<Verdict result={result} onReset={reset}/>:<div className="checker-body">{mode==='input'?<><div className="input-toggle"><button className={inputType==='link'?'selected':''} onClick={()=>setInputType('link')}>Link</button><button className={inputType==='message'?'selected':''} onClick={()=>setInputType('message')}>Message</button></div><label htmlFor="check-input">{inputType==='link'?'Paste a URL to analyze':'Paste a message to analyze'}</label>{inputType==='link'?<div className="input-row"><Link2 size={17}/><input id="check-input" value={value} onChange={e=>setValue(e.target.value)} placeholder="https://example.com/offer"/></div>:<textarea id="check-input" value={value} onChange={e=>setValue(e.target.value)} placeholder="Paste the suspicious message here..." rows={6}/>}</>:<><label>Upload a screenshot</label><input ref={fileRef} type="file" accept="image/*" hidden onChange={e=>{const next=e.target.files?.[0];if(next){setFile(next);setPreview(URL.createObjectURL(next))}}}/>{file?<div className="upload-preview"><img src={preview} alt="Selected screenshot preview"/><button onClick={()=>{setFile(null);setPreview('');if(fileRef.current)fileRef.current.value=''}} aria-label="Remove screenshot"><X size={16}/></button></div>:<button className="drop-zone" onClick={()=>fileRef.current?.click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();const next=e.dataTransfer.files[0];if(next?.type.startsWith('image/')){setFile(next);setPreview(URL.createObjectURL(next))}}}><UploadCloud size={24}/><strong>Drop an image here</strong><span>or click to browse</span></button>}</>}<button className="primary-button full" disabled={loading||(!value.trim()&&!file)} onClick={submit}>{loading?<><Loader2 className="spin" size={16}/> Checking signals...</>:<>Check for scams <ArrowRight size={16}/></>}</button><p className="privacy-note"><CheckCircle2 size={13}/> Nothing is stored without your permission</p></div>}</div>}
export function LandingPage(){return <div className="app-shell"><Header/><main><section className="hero"><div className="hero-copy"><div className="live-pill"><span/> LIVE PROTECTION FOR THE WEB</div><h1>Don&apos;t get <em>scammed.</em><br/>Get a second opinion.</h1><p className="hero-sub">Scam Detector spots subtle signs of fraud in links, messages, and checkout pages before you click, pay, or share.</p><div className="hero-actions"><a className="primary-button" href="#check">Check something now <ArrowRight size={16}/></a><a className="quiet-link" href="#how-it-works">See how it works <ArrowRight size={15}/></a></div></div><div className="hero-signal"><div className="signal-header"><span>● ANALYSIS ENGINE</span><span>v2.4.1</span></div><div className="signal-grid"><p className="eyebrow">SIGNAL STATUS</p><strong>Ready when you are</strong><div className="scan-lines"><i/><i/><i/><i/><i/></div></div><div className="signal-footer"><span>Private signal review</span><span>Updated today</span></div></div></section><section className="check-section" id="check"><div className="section-heading"><div><p className="eyebrow">THE QUICK CHECK</p><h2>Before you trust it,<br/><span>run it through us.</span></h2></div><p>Drop in anything that feels off. We&apos;ll explain what we find in plain English.</p></div><Checker/></section><HowItWorks/><section className="about-section" id="about"><p className="eyebrow">THE BROWSER EXTENSION</p><h2>Protection that stays close to the moment.</h2><p>Install the Scam Detector extension to check suspicious links and messages without leaving the page you are viewing. Open the popup, paste what feels off, and get a concise verdict with the signals that influenced it.</p><div className="extension-info"><div><span className="eyebrow">QUICK ACCESS</span><strong>One click from your toolbar</strong><p>Keep the detector available while browsing email, shopping pages, social feeds, and support chats.</p></div><div><span className="eyebrow">PRIVATE BY DEFAULT</span><strong>Review before you act</strong><p>The extension gives you context first, so you can decide what to share and what to ignore.</p></div><div><span className="eyebrow">BUILT FOR CLARITY</span><strong>Reasons, not just warnings</strong><p>Every result explains the suspicious patterns in plain language instead of relying on a vague red flag.</p></div></div><a className="primary-button" href="#extension">See extension preview <ArrowRight size={15}/></a></section></main><footer><Mark/><span>Built for the moment before you click.</span></footer></div>}
export function AppPage({title,children}:{title:string;children:React.ReactNode}){return <div className="app-shell"><Header/><main className="subpage"><p className="eyebrow">SCAM DETECTOR</p><h1>{title}</h1>{children}</main><footer><Mark/><span>Built for the moment before you click.</span></footer></div>}
export function LoginPage(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password.length<8){setError('Password must be at least 8 characters.');return}setLoading(true);const response=await signIn(email,password);if(response.error){setError(response.error);setLoading(false);return}sessionStorage.setItem('mock-session',JSON.stringify(response.user));router.push('/dashboard')};return <AppPage title="Welcome back"><form className="auth-card" onSubmit={submit}><p>Sign in to save checks and see your scan history.</p><label>Email address</label><input required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" type="email"/><label>Password</label><input required value={password} onChange={e=>setPassword(e.target.value)} placeholder="8 characters minimum" type="password"/><button className="primary-button full" disabled={loading}>{loading?'Signing in...':<>Sign in <ArrowRight size={15}/></>}</button>{error&&<p className="form-error">{error}</p>}<p className="muted center">New here? <Link href="/signup">Create an account</Link></p></form></AppPage>}
export function SignupPage(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();if(password.length<8)return setError('Password must be at least 8 characters.');if(password!==confirm)return setError('Passwords do not match.');setLoading(true);const response=await signUp(email,password);if(response.error){setError(response.error);setLoading(false);return}sessionStorage.setItem('mock-session',JSON.stringify(response.user));router.push('/dashboard')};return <AppPage title="Create your account"><form className="auth-card" onSubmit={submit}><p>Start checking suspicious links and messages with a private mock session.</p><label>Email address</label><input required value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com"/><label>Password</label><input required value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="8 characters minimum"/><label>Confirm password</label><input required value={confirm} onChange={e=>setConfirm(e.target.value)} type="password" placeholder="Repeat your password"/><button className="primary-button full" disabled={loading}>{loading?'Creating account...':<>Create account <ArrowRight size={15}/></>}</button>{error&&<p className="form-error">{error}</p>}<p className="muted center">Already have an account? <Link href="/login">Sign in</Link></p></form></AppPage>}
export function DemoDashboard(){const [empty,setEmpty]=useState(false);return <AppPage title="Your scan history"><div className="dashboard-toolbar"><p>Keep track of the things you&apos;ve checked.</p><label className="dev-toggle"><input type="checkbox" checked={empty} onChange={e=>setEmpty(e.target.checked)}/> Show empty state</label><a className="primary-button" href="/#check">New check <ArrowRight size={15}/></a></div>{empty?<div className="empty-state"><ShieldCheck size={36}/><h3>No checks yet</h3><p>Your next scan will appear here.</p><a className="primary-button" href="/#check">Run your first check</a></div>:<div className="history-card">{['prime-deal-alert.com/claim','support-paypal-help.net','your-bank-secure.com/login','wikipedia.org/article'].map((item,i)=><div className="history-row" key={item}><span className="history-item"><Link2 size={14}/>{item}</span><span className={i===3?'status-badge safe':'status-badge danger'}>{i===3?'SAFE':'HIGH RISK'}</span><span className="muted">{i+1} day ago</span></div>)}</div>}</AppPage>}
export function AdminPage(){const [denied,setDenied]=useState(false);return <AppPage title="Admin console"><label className="dev-toggle"><input type="checkbox" checked={denied} onChange={e=>setDenied(e.target.checked)}/> Simulate 403 access denied</label>{denied?<div className="empty-state"><X size={36}/><h3>Admin access only</h3><p>Your current mock role cannot view these statistics.</p></div>:<><div className="admin-grid"><div className="metric"><span>Checks today</span><strong>18,492</strong><small>+12.4% from yesterday</small></div><div className="metric"><span>High-risk rate</span><strong>31.8%</strong><small>Across all checks</small></div><div className="metric"><span>Model confidence</span><strong>94.2%</strong><small>Rolling 7 day average</small></div></div><div className="admin-card"><p className="eyebrow">RECENT SIGNALS</p><h3>Pattern activity</h3><div className="bar-chart">{[42,68,54,78,63,90,72,84,61,96,76,88].map((height,i)=><i key={i} style={{height:`${height}%`}}/>)}</div></div></>}</AppPage>}
